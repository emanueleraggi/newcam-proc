// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen

#include <string.h>
#include "senlcm_prosilica_attribute_t.h"

static int __senlcm_prosilica_attribute_t_hash_computed;
static uint64_t __senlcm_prosilica_attribute_t_hash;

uint64_t __senlcm_prosilica_attribute_t_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __senlcm_prosilica_attribute_t_get_hash)
            return 0;

    __lcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__senlcm_prosilica_attribute_t_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x12204fd2578d0266LL
         + __string_hash_recursive(&cp)
         + __string_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __senlcm_prosilica_attribute_t_get_hash(void)
{
    if (!__senlcm_prosilica_attribute_t_hash_computed) {
        __senlcm_prosilica_attribute_t_hash = (int64_t)__senlcm_prosilica_attribute_t_hash_recursive(NULL);
        __senlcm_prosilica_attribute_t_hash_computed = 1;
    }

    return __senlcm_prosilica_attribute_t_hash;
}

int __senlcm_prosilica_attribute_t_encode_array(void *buf, int offset, int maxlen, const senlcm_prosilica_attribute_t *p, int elements)
{
    int pos = 0, element;
    int thislen;

    for (element = 0; element < elements; element++) {

        thislen = __string_encode_array(buf, offset + pos, maxlen - pos, &(p[element].label), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __string_encode_array(buf, offset + pos, maxlen - pos, &(p[element].value), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int senlcm_prosilica_attribute_t_encode(void *buf, int offset, int maxlen, const senlcm_prosilica_attribute_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __senlcm_prosilica_attribute_t_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __senlcm_prosilica_attribute_t_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __senlcm_prosilica_attribute_t_encoded_array_size(const senlcm_prosilica_attribute_t *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __string_encoded_array_size(&(p[element].label), 1);

        size += __string_encoded_array_size(&(p[element].value), 1);

    }
    return size;
}

int senlcm_prosilica_attribute_t_encoded_size(const senlcm_prosilica_attribute_t *p)
{
    return 8 + __senlcm_prosilica_attribute_t_encoded_array_size(p, 1);
}

int __senlcm_prosilica_attribute_t_decode_array(const void *buf, int offset, int maxlen, senlcm_prosilica_attribute_t *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __string_decode_array(buf, offset + pos, maxlen - pos, &(p[element].label), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __string_decode_array(buf, offset + pos, maxlen - pos, &(p[element].value), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __senlcm_prosilica_attribute_t_decode_array_cleanup(senlcm_prosilica_attribute_t *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __string_decode_array_cleanup(&(p[element].label), 1);

        __string_decode_array_cleanup(&(p[element].value), 1);

    }
    return 0;
}

int senlcm_prosilica_attribute_t_decode(const void *buf, int offset, int maxlen, senlcm_prosilica_attribute_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __senlcm_prosilica_attribute_t_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __senlcm_prosilica_attribute_t_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int senlcm_prosilica_attribute_t_decode_cleanup(senlcm_prosilica_attribute_t *p)
{
    return __senlcm_prosilica_attribute_t_decode_array_cleanup(p, 1);
}

int __senlcm_prosilica_attribute_t_clone_array(const senlcm_prosilica_attribute_t *p, senlcm_prosilica_attribute_t *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __string_clone_array(&(p[element].label), &(q[element].label), 1);

        __string_clone_array(&(p[element].value), &(q[element].value), 1);

    }
    return 0;
}

senlcm_prosilica_attribute_t *senlcm_prosilica_attribute_t_copy(const senlcm_prosilica_attribute_t *p)
{
    senlcm_prosilica_attribute_t *q = (senlcm_prosilica_attribute_t*) malloc(sizeof(senlcm_prosilica_attribute_t));
    __senlcm_prosilica_attribute_t_clone_array(p, q, 1);
    return q;
}

void senlcm_prosilica_attribute_t_destroy(senlcm_prosilica_attribute_t *p)
{
    __senlcm_prosilica_attribute_t_decode_array_cleanup(p, 1);
    free(p);
}

int senlcm_prosilica_attribute_t_publish(lcm_t *lc, const char *channel, const senlcm_prosilica_attribute_t *p)
{
      int max_data_size = senlcm_prosilica_attribute_t_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = senlcm_prosilica_attribute_t_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _senlcm_prosilica_attribute_t_subscription_t {
    senlcm_prosilica_attribute_t_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void senlcm_prosilica_attribute_t_handler_stub (const lcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    senlcm_prosilica_attribute_t p;
    memset(&p, 0, sizeof(senlcm_prosilica_attribute_t));
    status = senlcm_prosilica_attribute_t_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding senlcm_prosilica_attribute_t!!!\n", status);
        return;
    }

    senlcm_prosilica_attribute_t_subscription_t *h = (senlcm_prosilica_attribute_t_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    senlcm_prosilica_attribute_t_decode_cleanup (&p);
}

senlcm_prosilica_attribute_t_subscription_t* senlcm_prosilica_attribute_t_subscribe (lcm_t *lcm,
                    const char *channel,
                    senlcm_prosilica_attribute_t_handler_t f, void *userdata)
{
    senlcm_prosilica_attribute_t_subscription_t *n = (senlcm_prosilica_attribute_t_subscription_t*)
                       malloc(sizeof(senlcm_prosilica_attribute_t_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel,
                                 senlcm_prosilica_attribute_t_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg senlcm_prosilica_attribute_t LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int senlcm_prosilica_attribute_t_subscription_set_queue_capacity (senlcm_prosilica_attribute_t_subscription_t* subs,
                              int num_messages)
{
    return lcm_subscription_set_queue_capacity (subs->lc_h, num_messages);
}

int senlcm_prosilica_attribute_t_unsubscribe(lcm_t *lcm, senlcm_prosilica_attribute_t_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe senlcm_prosilica_attribute_t_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

