/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __herc_acq_camera_t_hpp__
#define __herc_acq_camera_t_hpp__

#include <string>
#include <vector>
#include "herc_acq/attribute_t.hpp"

namespace herc_acq
{

class camera_t
{
    public:
        int64_t    utime;

        std::string camID;

        std::string triggerID;

        std::string surveyID;

        std::string outputPath;

        int32_t    n_attributes;

        std::vector< herc_acq::attribute_t > attributes;

        int32_t    state;

    public:
        enum { STATE_RUNNING = 0 };
        enum { STATE_READY = 1 };
        enum { STATE_WAITING_FOR_CAMERA = 2 };
        enum { STATE_WAITING_FOR_SURVEY = 3 };

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to reqad while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "camera_t"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int camera_t::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = (int64_t)getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int camera_t::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int camera_t::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t camera_t::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* camera_t::getTypeName()
{
    return "camera_t";
}

int camera_t::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->utime, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* camID_cstr = (char*) this->camID.c_str();
    tlen = __string_encode_array(buf, offset + pos, maxlen - pos, &camID_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* triggerID_cstr = (char*) this->triggerID.c_str();
    tlen = __string_encode_array(buf, offset + pos, maxlen - pos, &triggerID_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* surveyID_cstr = (char*) this->surveyID.c_str();
    tlen = __string_encode_array(buf, offset + pos, maxlen - pos, &surveyID_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* outputPath_cstr = (char*) this->outputPath.c_str();
    tlen = __string_encode_array(buf, offset + pos, maxlen - pos, &outputPath_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->n_attributes, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    for (int a0 = 0; a0 < this->n_attributes; a0++) {
        tlen = this->attributes[a0]._encodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->state, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int camera_t::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->utime, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t __camID_len__;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &__camID_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__camID_len__ > maxlen - pos) return -1;
    this->camID.assign(((const char*)buf) + offset + pos, __camID_len__ - 1);
    pos += __camID_len__;

    int32_t __triggerID_len__;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &__triggerID_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__triggerID_len__ > maxlen - pos) return -1;
    this->triggerID.assign(((const char*)buf) + offset + pos, __triggerID_len__ - 1);
    pos += __triggerID_len__;

    int32_t __surveyID_len__;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &__surveyID_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__surveyID_len__ > maxlen - pos) return -1;
    this->surveyID.assign(((const char*)buf) + offset + pos, __surveyID_len__ - 1);
    pos += __surveyID_len__;

    int32_t __outputPath_len__;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &__outputPath_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__outputPath_len__ > maxlen - pos) return -1;
    this->outputPath.assign(((const char*)buf) + offset + pos, __outputPath_len__ - 1);
    pos += __outputPath_len__;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->n_attributes, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->attributes.resize(this->n_attributes);
    for (int a0 = 0; a0 < this->n_attributes; a0++) {
        tlen = this->attributes[a0]._decodeNoHash(buf, offset + pos, maxlen - pos);
        if(tlen < 0) return tlen; else pos += tlen;
    }

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->state, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int camera_t::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += this->camID.size() + 4 + 1;
    enc_size += this->triggerID.size() + 4 + 1;
    enc_size += this->surveyID.size() + 4 + 1;
    enc_size += this->outputPath.size() + 4 + 1;
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    for (int a0 = 0; a0 < this->n_attributes; a0++) {
        enc_size += this->attributes[a0]._getEncodedSizeNoHash();
    }
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    return enc_size;
}

uint64_t camera_t::_computeHash(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for(fp = p; fp != NULL; fp = fp->parent)
        if(fp->v == camera_t::getHash)
            return 0;
    const __lcm_hash_ptr cp = { p, (void*)camera_t::getHash };

    uint64_t hash = 0x73e84fb02b4efda0LL +
         herc_acq::attribute_t::_computeHash(&cp);

    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
